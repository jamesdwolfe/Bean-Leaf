<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.beanleaf">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".state"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".viewCharts"></activity>
        <activity android:name=".accountInfo" />
        <!--
     You don't need to include android:required="false" if your app's
     minSdkVersion is 28 or higher.
        -->
        <uses-library
            android:name="android.test.runner"
            android:required="false" />

        <activity android:name=".viewMenuItem2" />
        <activity android:name=".viewMenu2" />
        <activity android:name=".viewRestaurantDetails" />
        <activity android:name=".itemDetails" />
        <activity android:name=".viewMenu" />
        <activity android:name=".viewMenuItem" />
        <activity android:name=".orderDetails" />
        <activity android:name=".orderHistory" />
        <activity android:name=".merchantPage2" />
        <activity android:name=".viewRestaurants" />
        <activity android:name=".addRequests" />
        <activity android:name=".restaurantPage" />
        <activity
            android:name=".CloseEnough"
            android:label="@string/title_activity_close_enough"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".request" />
        <activity android:name=".administrator" />
        <activity android:name=".addResturant" />
        <activity android:name=".BarChartActivity" />
        <activity android:name=".BarChartActivity2" />
        <activity android:name=".customerPage" />
        <activity android:name=".merchantPage" />
        <activity android:name=".signIn" />
        <activity android:name=".MenuItem" />
        <activity android:name=".regsiter" />
        <activity
            android:name=".NavigationActivity"
            android:label="@string/title_activity_navigation"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Pop"
            android:label="menu2"
            android:theme="@style/AppTheme.CustomTheme" />
    </application>

</manifest>